# 参考参数生成器设计文档

## 概述

参考参数生成器（ReferenceParamsGenerator）是系统的核心组件，负责根据用例配置和音频标注数据，自动生成测试所需的参考参数。

## 数据流程

```
┌─────────────────┐     ┌──────────────────┐     ┌─────────────────────────┐
│ AlgorithmRefe   │     │ TestCase.config  │     │ AudioAnnotation         │
│ renceParam      │     │ .audios          │     │ .code                   │
│ (算法配置定义)   │     │ (用例音频配置)    │     │ (diarization/asr/      │
│                 │     │                  │     │  translation)           │
└────────┬────────┘     └────────┬─────────┘     └───────────┬─────────────┘
         │                       │                             │
         │   读取定义           │  读取音频ID                 │  查询标注数据
         ▼                       ▼                             ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                      ReferenceParamsGenerator                            │
│  1. 根据 algorithm_type 读取参考参数配置                                 │
│  2. 从 config.audios 获取音频ID列表                                     │
│  3. 查询 AudioAnnotation 表获取标注数据                                 │
│  4. 处理重叠播放的时间戳调整                                            │
│  5. 生成参考参数 (text/json/rttm/stm)                                  │
└─────────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                         reference_params                                │
│  [                                                                        │
│    {"code": "asr_reference_text", "type": "text", "api": "...", ...},  │
│    {"code": "translation_reference_text", "type": "text", "api": "..."},
│    {"code": "diarization_rttm", "type": "rttm", "api": {...}, ...}    │
│  ]                                                                        │
└─────────────────────────────────────────────────────────────────────────┘
```

## 核心数据结构

### 1. AlgorithmReferenceParam（算法参考参数配置）

存储在 `algorithm_reference_params` 表中，定义每个算法的参考参数结构：

| 字段 | 说明 | 示例 |
|------|------|------|
| `algorithm_type` | 算法类型 | `asr`, `translation`, `speaker_recognition` |
| `code` | 参考参数代码（用户自定义） | `my_asr_text`, `custom_diarization` |
| `param_type` | 参数类型 | `text`, `json`, `rttm`, `stm` |
| `annotation_code` | 标注代码 | `asr`, `translation`, `diarization` |
| `annotation_format` | 标注格式 | `json`, `text`, `rttm`, `stm` |

### 2. AudioAnnotation（音频标注数据）

存储在 `audio_annotations` 表中：

| 字段 | 说明 | 示例 |
|------|------|------|
| `audio_id` | 关联音频ID | `4`, `17` |
| `code` | 标注代码 | `asr`, `translation`, `diarization` |
| `format` | 标注格式 | `json`, `text`, `rttm`, `stm` |
| `data` | 标注数据（JSON） | `{"segments": [...], "text": "..."}` |

### 3. TestCase.config（用例配置）

```python
{
    "audios": [
        {"audio_id": 4, "play_order": 1, "test_type": "api"},
        {"audio_id": 17, "play_order": 2, "test_type": "api"}
    ],
    "overlap_rate": 0.25,  # 25% 重叠率
    "algorithm_type": "asr"
}
```

## 生成器架构

### 类结构

```
BaseReferenceGenerator (ABC)
├── TranslationASRGenerator      # 翻译算法 - ASR参考文本
├── TranslationTextGenerator      # 翻译算法 - 翻译参考文本
├── TranslationRTTMGenerator     # 翻译算法 - RTTM标注
├── TranslationSTMGenerator      # 翻译算法 - STM标注
├── ASRGenerator                # ASR算法 - ASR参考文本
├── ASRRTTMGenerator            # ASR算法 - RTTM标注
├── ASRSTMGenerator             # ASR算法 - STM标注
├── TTSGenerator                # TTS算法
├── SpeakerRecognitionGenerator  # 说话人识别算法
└── SpeakerRecognitionRTTMGenerator

ReferenceParamsGenerator (主类)
├── generate(test_case)         # 生成参考参数列表
├── apply_to_config(test_case) # 应用到用例配置
└── get_reference_text(...)     # 从配置获取参考文本
```

### 算法类型映射

```python
_generators = {
    'translation': [TranslationASRGenerator, TranslationTextGenerator,
                    TranslationRTTMGenerator, TranslationSTMGenerator],
    'asr': [ASRGenerator, ASRRTTMGenerator, ASRSTMGenerator],
    'tts': [TTSGenerator],
    'speaker_recognition': [SpeakerRecognitionGenerator, SpeakerRecognitionRTTMGenerator],
}
```

## 生成逻辑详解

### 1. 文本类型参考参数（text/json）

**处理逻辑**：
1. 从 `config.audios` 按 `play_order` 排序
2. 逐个查询音频的标注数据
3. 直接拼接所有文本内容

**示例**：
```python
# 音频4的ASR文本: "你好世界"
# 音频17的ASR文本: "Hello World"
# 合并后: "你好世界 Hello World "
```

### 2. 结构化标注（RTTM/STM）

**处理逻辑**：
1. 计算每个音频的时间偏移（支持重叠播放）
2. 调整每个片段的时间戳
3. 合并所有片段并按时间排序
4. 转换为 RTTM/STM 格式

#### 2.1 重叠播放时间偏移计算

```python
def _calculate_audio_offsets(audios_config, overlap_rate):
    """
    假设:
    - 音频1时长: 10s
    - 音频2时长: 10s
    - 重叠率: 0.25 (25%)
    
    计算结果:
    - 音频1偏移: 0s
    - 音频2偏移: 10 * (1 - 0.25) = 7.5s
    """
```

#### 2.2 时间戳调整

```python
def _adjust_segment_timestamps(segments, offset):
    # 每个片段的 start 和 end 加上偏移量
    for seg in segments:
        seg['start'] = seg['start'] + offset
        seg['end'] = seg['end'] + offset
```

#### 2.3 RTTM 格式

```
SPEAKER test 1 0.00 3.50 <NA> <NA> spk0 <NA> <NA>
文本内容
SPEAKER test 1 7.50 3.50 <NA> <NA> spk1 <NA> <NA>
文本内容
```

#### 2.4 STM 格式

```
test spk0 0.00 3.50 文本内容
test spk1 7.50 3.50 文本内容
```

## 使用示例

### 1. 基本用法

```python
from backend.algorithm.reference_params_generator import ReferenceParamsGenerator
from backend.models.database import db

# 假设有一个测试用例
test_case = db.session.get(TestCase, case_id)

# 生成参考参数
ReferenceParamsGenerator.apply_to_config(test_case)
db.session.commit()

# 查看生成的参考参数
print(test_case.reference_params)
```

### 2. 手动构造配置

```python
from backend.algorithm.reference_params_generator import ReferenceParamsGenerator
from backend.models.models import TestCase


# 创建虚拟用例对象
class MockTestCase:
    def __init__(self):
        self.algorithm_type = 'asr'
        self.config = {
            "audios": [
                {"audio_id": 4, "play_order": 1, "test_type": "api"},
                {"audio_id": 17, "play_order": 2, "test_type": "api"}
            ],
            "overlap_rate": 0.25
        }
        self.reference_params = []


mock_case = MockTestCase()
ReferenceParamsGenerator.apply_to_config(mock_case)

print(mock_case.reference_params)
```

### 3. 获取特定参考文本

```python
from backend.algorithm.reference_params_generator import ReferenceParamsGenerator

config = {
    "audios": [
        {"audio_id": 4, "play_order": 1, "test_type": "api"},
        {"audio_id": 17, "play_order": 2, "test_type": "api"}
    ]
}

# 获取 ASR 参考文本
asr_text = ReferenceParamsGenerator.get_reference_text(config, 'asr_reference_text', 'api')

# 获取翻译参考文本
trans_text = ReferenceParamsGenerator.get_reference_text(config, 'translation_reference_text', 'api')
```

## 输出格式

### reference_params 结构

```python
[
    {
        "code": "asr_reference_text",      # 参考参数代码
        "type": "text",                     # 参数类型
        "api": "你好世界 Hello World ",    # API测试参考值
        "e2e": "你好世界 Hello World ",    # E2E测试参考值
        "annotation_code": "asr",          # 标注代码
        "annotation_format": "json"         # 标注格式
    },
    {
        "code": "translation_reference_text",
        "type": "text",
        "api": "翻译文本...",
        "e2e": "翻译文本...",
        "annotation_code": "translation",
        "annotation_format": "json"
    },
    {
        "code": "diarization_rttm",
        "type": "rttm",
        "api": {
            "segments": [...],
            "format": "rttm",
            "text": "SPEAKER test 1 0.00...",
            "json": "[...]"
        },
        "e2e": {...},
        "annotation_code": "diarization",
        "annotation_format": "rttm"
    }
]
```

## 调用时机

参考参数生成器在以下场景被调用：

1. **创建用例时** - `testcase_controller.py`
   - 用户创建测试用例后，自动生成参考参数

2. **导入用例时** - `audio_controller.py`
   - 批量导入音频生成用例时触发

3. **执行测试时** - `api_executor.py`, `task_controller.py`
   - 获取参考文本用于评分计算

4. **导出报告时** - 各种 report_controller
   - 读取参考参数生成测试报告
