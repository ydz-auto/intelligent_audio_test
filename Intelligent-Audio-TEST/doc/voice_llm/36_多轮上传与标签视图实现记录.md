# 36 - 多轮上传与标签视图实现记录

> **实现日期**：2026-07-06
> **冲刺范围**：三个断层问题修复 + 标签视图 + 标注任意字段显示
> **验证状态**：后端 12/12 测试全绿，前端构建成功

---

## 1. 背景与问题

在 voice_llm 改造的第 2 步（选用例）中，发现三个断层问题：

| 断层 | 现象 | 根因 |
|------|------|------|
| 分组能力半成品 | `groupNameType`/`customGroupName`/`inheritTags` 字段定义了但控制器未使用 | 字段未接入用例创建逻辑 |
| 多轮用例上传不支持 | `_create_test_case_from_audio` 只生成平面 config，无 `rounds` | 上传流程未透传 rounds 配置 |
| 标注→参考参数链路断开 | `apply_to_config` 因 `rounds` 为空而 no-op | 上传时未构建 rounds，导致参考参数不生成 |

**额外遗漏**：音频详情模态窗（DetailViewModal）不支持标注任意字段显示。

---

## 2. 解决方案

### 2.1 分组能力 → 标签视图方案

**决策**：不改分组逻辑，给用例管理增加标签视图（`view=tag`）。

**理由**：
- 分组（TestCaseGroup）已有完整 CRUD，再改会造成逻辑混乱
- 用户真正需要的是"按标签维度查看用例"
- 标签视图是只读聚合视图，不影响数据结构

### 2.2 多轮用例上传 → rounds 透传

**架构**：

```
前端文件夹选择 → folderParser.ts 解析 → 构建 testCaseConfig.rounds
→ mergeChunks API 透传 → _create_test_case_from_audio(rounds_config=...)
→ config = { rounds: [...] } → ReferenceParamsGenerator.apply_to_config(tc)
```

**两种轮次模式**：

| 模式 | 触发条件 | rounds 结构 |
|------|---------|------------|
| `multi_round` | 单个音频的最子级文件夹 | 每个音频 = 一轮 |
| `single_round_multi_audio` | 多个音频的最子级文件夹 | 多音频同轮 |

**标注文件位置规则**：最子级文件夹的父级目录下，文件名 = 最子级文件夹名。

### 2.3 标注→参考参数 → 同步生成

**时机**：`_create_test_case_from_audio` 中，rounds 模式下同步调用 `ReferenceParamsGenerator.apply_to_config(new_tc)`。

**文件命名**：`{case_id}_round_{n}.json`，逐轮生成。

### 2.4 标注任意字段显示 → 动态列

**常量**：

```typescript
const KNOWN_SEGMENT_FIELDS = ['speaker', 'start', 'end', 'text', 'duration', ...]
const KNOWN_DATA_KEYS = ['segments', 'text', 'annotations', 'timestamps', 'timestamps_global']
```

**策略**：已知字段专门列渲染，额外字段动态检测并显示为额外列 / 键值对编辑。

---

## 3. 改动文件清单

### 后端（5 个文件）

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `backend/schemas/audio.py` | 修改 | 新增 `TestCaseUploadConfig` schema + `MergeChunksRequest.test_case_config` 字段 |
| `backend/controllers/audio_controller.py` | 修改 | 新增 `_build_rounds_from_files`；重构 `_create_test_case_from_audio` 支持 rounds；`merge_chunks` 两处透传 |
| `backend/controllers/testcase_controller.py` | 修改 | 新增 `view=tag` 标签视图 API + `_get_tag_view` 方法 |
| `backend/tests/test_audio_upload_rounds.py` | 新增 | 10 个测试覆盖 schema、_build_rounds、_create_test_case |
| `backend/tests/test_testcase_tag_view.py` | 新增 | 2 个测试覆盖标签视图 API |

### 前端（7 个文件）

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `frontend/src/store/testCaseStore.ts` | 修改 | 新增 `tagViewData`/`tagViewPagination` 状态 + `fetchTagView` 方法 |
| `frontend/src/views/TestCaseManagerLogic/testCaseManager.ts` | 修改 | 新增 `viewMode` ref + `refreshCurrentView` + watch 切换 |
| `frontend/src/views/TestCaseManager.vue` | 修改 | 传递 `tagViewData` 和 `v-model:viewMode` |
| `frontend/src/components/common/test-case/TestCaseListContainer.vue` | 修改 | 视图切换器 + 标签视图计算属性 + 标签视图模板 |
| `frontend/src/components/common/modal/DetailViewModal.vue` | 修改 | KNOWN 常量 + 动态额外列 + data 顶层字段编辑 |
| `frontend/src/utils/folderParser.ts` | **新增** | 文件夹解析工具：extractAudioFiles、buildRoundsConfig、buildTestCaseConfig |
| `frontend/src/views/AudioImportLogic/audioImport.ts` | 修改 | 集成 folderParser，构建 testCaseConfig 并传入 mergeChunks |

---

## 4. 关键代码片段

### 4.1 后端 - TestCaseUploadConfig Schema

```python
class TestCaseUploadConfig(APIModel):
    """上传时携带的测试用例配置，支持多轮 rounds 架构。"""
    rounds: Optional[List[Dict[str, Any]]] = Field(None, alias='rounds')
    dimensions: Optional[List[Dict]] = Field(None, alias='dimensions')
    group_name: Optional[str] = Field(None, alias='groupName')
    inherit_tags: Optional[bool] = Field(True, alias='inheritTags')
    algorithm_params: Optional[List[AlgorithmParamItem]] = Field(None, alias='algorithmParams')

class MergeChunksRequest(APIModel):
    # ... 原有字段 ...
    test_case_config: Optional[TestCaseUploadConfig] = Field(None, alias='testCaseConfig')
```

### 4.2 后端 - _build_rounds_from_files

```python
@staticmethod
def _build_rounds_from_files(files, mode="multi_round", spl=65.0, playback_device_id=None):
    if not files:
        return []
    def _make_audio_config(item, play_order):
        cfg = {"audio_id": item["audio_id"], "spl": spl, "play_order": play_order}
        if playback_device_id:
            cfg["playback_device_id"] = playback_device_id
        return cfg
    if mode == "single_round_multi_audio":
        audios = [_make_audio_config(item, idx) for idx, item in enumerate(files)]
        return [{"roundNumber": 1, "audios": audios}]
    else:
        rounds = []
        for idx, item in enumerate(files):
            rounds.append({"roundNumber": idx + 1, "audios": [_make_audio_config(item, 0)]})
        return rounds
```

### 4.3 后端 - _create_test_case_from_audio（rounds 分支）

```python
if rounds_config:
    config = {"source_audio": audio.name, "auto_generated": True, "rounds": rounds_config}
    # dimensions 处理...
else:
    # 平面模式（向后兼容）
    config = {"source_audio": audio.name, "auto_generated": True, "audios": [audio_config]}

# inherit_tags
if inherit_tags:
    for tag_name in audio_tags:
        tag = Tag.query.filter_by(name=tag_name).first()
        if tag:
            new_tc.tags.append(tag)

# 同步生成参考参数
from backend.utils.algorithm.reference_params_generator import ReferenceParamsGenerator
ReferenceParamsGenerator.apply_to_config(new_tc)
```

### 4.4 后端 - 标签视图

```python
# testcase_controller.py
def get_all():
    view = request.args.get('view')
    if view == 'tag':
        return TestCaseController._get_tag_view(
            page=page, per_page=per_page, keyword=keyword,
            test_type=test_type, algorithm_type=algorithm_type,
            include_deleted=include_deleted,
        )
    # ... 原有分组视图逻辑 ...

@staticmethod
def _get_tag_view(page=1, per_page=10, ...):
    """标签视图：按标签聚合用例，分页返回标签维度。"""
    tag_pagination = Tag.query.order_by(Tag.name).paginate(...)
    items = []
    for tag in tag_pagination.items:
        tc_query = TestCase.query.join(TestCase.tags).filter(Tag.id == tag.id)
        # 过滤逻辑...
        items.append({"tag": tag.name, "testCases": case_list})
    return success_response({"items": items, "total": tag_pagination.total, ...})
```

### 4.5 前端 - folderParser.ts 核心函数

```typescript
// 判定轮次模式
export function determineRoundMode(audioFiles: AudioFileInfo[]) {
  return audioFiles.length === 1 ? 'multi_round' : 'single_round_multi_audio'
}

// 构建 rounds 配置
export function buildRoundsConfig(audioFiles, allFiles, spl, playbackDeviceId) {
  const mode = determineRoundMode(audioFiles)
  if (mode === 'single_round_multi_audio') {
    return [{ roundNumber: 1, audios: audioFiles.map((a, i) => ({ audio_name: a.name, play_order: i })) }]
  } else {
    return audioFiles.map((audio, idx) => ({
      roundNumber: idx + 1,
      audios: [{ audio_name: audio.name, play_order: 0 }]
    }))
  }
}
```

### 4.6 前端 - audioImport.ts 集成

```typescript
// startUploadProcess 中构建 testCaseConfig
const allRawFiles = files.map(f => f.file || f)
const audioFileInfos = extractAudioFiles(allRawFiles)
const audioGroups = groupAudioFilesByLeafFolder(audioFileInfos)

if (audioFileInfos.length > 0 && uploadOptions.createTestCase) {
  const allRounds = []
  let roundNumber = 1
  audioGroups.forEach(groupFiles => {
    const groupConfig = buildTestCaseConfig(groupFiles, allRawFiles, {...})
    if (groupConfig.rounds) {
      groupConfig.rounds.forEach(r => allRounds.push({ ...r, roundNumber: roundNumber++ }))
    }
  })
  if (allRounds.length > 0) {
    testCaseConfig = { rounds: allRounds, group_name: ..., inherit_tags: ... }
  }
}

// 透传到 mergeChunks
await processMergeForExistingFile(taskId, fileTask, uploadOptions, testCaseConfig)
await uploadFileChunks(taskId, fileTask, uploadOptions, testCaseConfig)
```

### 4.7 前端 - DetailViewModal 动态列

```typescript
const KNOWN_SEGMENT_FIELDS = ['speaker', 'start', 'end', 'text', 'duration', ...]
const KNOWN_DATA_KEYS = ['segments', 'text', 'annotations', 'timestamps', 'timestamps_global']

const extraSegmentFields = computed(() => {
  // 检测 segments 中的非已知字段
  const fieldSet = new Set()
  segments.forEach(seg => {
    Object.keys(seg).forEach(key => {
      if (!KNOWN_SEGMENT_FIELDS.includes(key)) fieldSet.add(key)
    })
  })
  return Array.from(fieldSet)
})

const extraDataFields = computed(() => {
  // data 顶层的非已知字段
  return Object.keys(ann.data).filter(key => !KNOWN_DATA_KEYS.includes(key))
})
```

---

## 5. 测试覆盖

### 后端测试（12/12 全绿）

| 测试文件 | 测试类 | 测试数 | 覆盖内容 |
|---------|--------|--------|---------|
| `test_audio_upload_rounds.py` | `TestMergeChunksRequestRounds` | 3 | rounds config、default none、annotation scope |
| | `TestBuildRoundsFromFiles` | 4 | multi_round、single_round_multi_audio、空文件、单文件 |
| | `TestCreateTestCaseWithRounds` | 3 | explicit rounds、fallback flat、inherit_tags=False |
| `test_testcase_tag_view.py` | — | 2 | tag view grouped by tag、default view unchanged |

### 前端验证

| 验证项 | 结果 |
|--------|------|
| TypeScript 类型检查（vue-tsc --noEmit） | 无新增错误 |
| Vite 生产构建（3 次） | 全部成功 |
| TestCaseListContainer 打包 | 52.11 kB |
| AudioImport 打包 | 52.84 kB |

---

## 6. 与设计文档的对应关系

| 本次实现 | 对应设计文档 | 状态更新 |
|---------|------------|---------|
| 多轮上传 rounds 透传 | `02_选用例/backend/03_Config_JSON扁平化设计` | rounds 架构已落地到上传流程 |
| 标注→参考参数同步生成 | `02_选用例/backend/10_reference_params_generator适配` | 上传时自动触发已实现 |
| 标签视图 | （新增，原设计无） | 补充为分组能力的替代方案 |
| 标注任意字段显示 | （新增，原设计无） | 补充 DetailViewModal 改造 |
| folderParser.ts 文件夹解析 | `02_选用例/frontend/23_UploadOptions_test_type` | 扩展为文件夹级多轮解析 |

---

## 7. 后续待办

| 项 | 说明 | 优先级 |
|----|------|--------|
| RoundConfigEditor 前端编辑器 | 设计文档 `10_RoundConfigEditor.md` 描述的核心编辑器，本次未实现 | 高 |
| CaseForm 重构为轮次为核心 | 设计文档 `06_CaseForm_test_type驱动.md`，当前 CaseForm 仍是平面结构 | 高 |
| 前端 E2E 功能测试 | 标签视图切换、多轮上传、DetailViewModal 动态列的浏览器实际验证 | 中 |
| 前后端联调 | testCaseConfig 字段从前端到后端的完整链路验证 | 中 |
| 数据迁移脚本 | 旧 config → rounds[] 格式转换 | 低（新用例不需要） |

---

## 8. 相关文档

- [README.md](README.md) — 文档导航索引
- [00_改造方案总览.md](00_改造方案总览.md) — 改造前后流程图
- [02_选用例/00_步骤总览.md](02_选用例/00_步骤总览.md) — 选用例步骤总览
- [02_选用例/backend/10_reference_params_generator适配.md](02_选用例/backend/10_reference_params_generator适配.md) — 参考参数生成设计
- [开发计划.md](开发计划.md) — 开发计划与工时
