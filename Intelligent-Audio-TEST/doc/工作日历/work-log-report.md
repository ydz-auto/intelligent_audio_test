# 报告工作记录（2026-07 ~ 2026-08）

> 统计范围：2026-07-01 ~ 2026-08-31，报告模块共涉及 **27 次提交**（主改动 15 次）。
> 包含：后端报告接口、前端报告页（TestReports/HistoryReports/ReportView）。
> 人员分布：lijiahao(21), ydz-auto(6)

## 一、总览

| 子模块 | 涉及提交 | 时间跨度（活跃天数） | 人员 |
|---|---|---|---|
| 后端报告接口 | 20 | 07-06 ~ 08-29（13 天） | lijiahao(16), ydz-auto(4) |
| 前端报告页 | 17 | 07-06 ~ 08-31（13 天） | lijiahao(11), ydz-auto(6) |

**报告能力演进**：多轮报告展示 → pass_rate 聚合 → 报告对比/HTML 导出 → 用例详情重构（维度 tab）→ 维度层级分组显示。

---

## 二、后端报告接口明细

### 7 月：多轮报告基础

| 日期 | 作者 | 工作内容 |
|---|---|---|
| 07-06 | lijiahao | **维度输出字段配置**（报告数据来源基础）+ 用例标签视图性能优化 |
| 07-06 | lijiahao | 标签视图修复（报告入口适配） |
| 07-10 | lijiahao | B1-B4 消除重复代码（report 控制器） |
| 07-10 | lijiahao | 多轮评估轮次内即采即评 + 预创建 TestResult（报告数据结构定型） |
| 07-13 | lijiahao | LLM Judge 多模态评估（报告字段链路） |
| 07-16 | lijiahao | 修复小艺录屏启动误判（同步 report_utils 修复） |
| 07-17 | lijiahao | **多轮报告展示优化及 camelCase 兼容** |
| 07-21 | lijiahao | **排除用例评估结果的无效字段**（报告数据瘦身） |
| 07-27 | ydz-auto | 优化前端视图实现（报告接口适配） |

### 8 月：聚合策略与导出能力

| 日期 | 作者 | 工作内容 |
|---|---|---|
| 08-14 | lijiahao | 前后端 camelCase 命名统一（报告字段层） |
| 08-17 | lijiahao | **新增 pass_rate 聚合策略：达标用例数/总用例数** |
| 08-17 | lijiahao | env_judge 模块重构（报告维度适配） |
| 08-23 | ydz-auto | **报告对比 / HTML 导出 / 字段类型时间戳 / 用例导出工具 + 数据迁移脚本** |
| 08-23 | lijiahao | 标签筛选支持 algorithm_type 过滤 + **通过率统计排除 null 维度值** |
| 08-24 | ydz-auto | **报告用例详情重构：维度 tab 切换 + 设备/API 结果独立分区**（后端接口支撑） |
| 08-24 | lijiahao | pcm 缓存清理重构（报告音频资源链路） |
| 08-25 | lijiahao | 报告对比页面搜索支持用例 ID（后端查询支持） |
| 08-26 | lijiahao | **报告后端分页** + 端口配置统一 |
| 08-28 | lijiahao | **维度层级全链路贯通 + 报告分组显示** |
| 08-28 | lijiahao | 移除维度层级富化方法，回退为内联 decimal_places 补充（方案收敛） |
| 08-28 | ydz-auto | 维度层级分组显示修复：主维度行兼作标题行 + **后端补回父维度** |
| 08-29 | lijiahao | **多轮评估维度隔离与报告聚合** |
| 08-29 | lijiahao | 修复多轮报告执行结果快照缺失轮次标记的问题 |

---

## 三、前端报告页明细

### 7 月：多轮报告展示

| 日期 | 作者 | 工作内容 |
|---|---|---|
| 07-06 | lijiahao | 标签视图修复（报告页适配） |
| 07-17 | lijiahao | **多轮报告展示优化** |
| 07-21 | lijiahao | 排除无效字段（前端展示适配） |
| 07-27 | ydz-auto | 前端视图优化（报告页） |

### 8 月：对比、导出与层级化重构

| 日期 | 作者 | 工作内容 |
|---|---|---|
| 08-14 | lijiahao | camelCase 命名统一（报告前端适配） |
| 08-17 | lijiahao | pass_rate 聚合策略展示 + env_judge 适配 |
| 08-23 | ydz-auto | **报告对比 / HTML 导出 / 用例导出工具（前端入口）** |
| 08-24 | ydz-auto | **报告用例详情重构：维度 tab 切换 + 设备/API 结果独立分区 + 音频播放按钮** |
| 08-25 | lijiahao | 报告对比页面搜索支持用例 ID |
| 08-26 | lijiahao | 报告后端分页（前端适配） |
| 08-27 | ydz-auto | 重估功能优化（报告入口） |
| 08-28 | ydz-auto | **维度层级分组显示修复：主维度行兼作标题行 + camelCase 兼容** |
| 08-28 | lijiahao | **维度层级全链路贯通 + 报告分组显示 + 评估无限滚动** |
| 08-28 | ydz-auto | 评估结果 tab 子维度归入父维度组，避免子维度独立成 tab |
| 08-28 | lijiahao | **对比报告轮次 Tab + 维度层级分组 + CaseIdBadge 组件复用 + 进度计算修复** |
| 08-28 | ydz-auto | 标签级联删除（报告页入口适配） |
| 08-29 | lijiahao | 多轮评估维度隔离与报告聚合（前端优化） |
| 08-31 | lijiahao | 修复分组勾选框无法联动选中分组下所有用例（报告筛选项） |

---

## 四、关键特性落地时间线

| 特性 | 日期 | 作者 | 说明 |
|---|---|---|---|
| 维度输出字段配置 | 07-06 | lijiahao | 报告可配置化基础 |
| 多轮报告展示优化 | 07-17 | lijiahao | camelCase 兼容 |
| pass_rate 聚合策略 | 08-17 | lijiahao | 达标用例数/总用例数，与 average 并列 |
| 报告对比 + HTML 导出 | 08-23 | ydz-auto | 双报告对比视图、HTML 离线导出、用例导出 |
| 报告用例详情重构 | 08-24 | ydz-auto | 维度 tab 切换、设备/API 独立分区、音频播放按钮 |
| 维度层级分组显示 | 08-28 | lijiahao + ydz-auto | 主子维度归组、父维度补回、主维度行兼作标题行 |
| 对比报告轮次 Tab | 08-28 | lijiahao | 对比页按轮次切换 + CaseIdBadge 复用 |

---

## 五、关键结论

1. **报告模块集中在月末爆发**：27 次提交中 18 次发生在 08-23 之后，与「评估维度体系（turn_taking/interruption/env_judge）成型」直接相关——维度多了，报告的分组/tab/对比需求随之而来。
2. **四项核心能力上线**：聚合策略多样化（average + pass_rate）、报告对比、HTML/用例导出、维度层级化展示。
3. **前后端协同紧密**：8-28 一天内 5 次提交完成维度层级「后端补父维度 → 前端主维度行兼作标题行 → 子维度归父组」全链路贯通，随后 8-29 即回退富化方法收敛方案（避免过度设计）。
4. **人员集中**：lijiahao 主导后端接口与数据结构，ydz-auto 主导前端交互重构。

---
*生成时间：2026-09-01，基于 git 提交记录自动统计。*
