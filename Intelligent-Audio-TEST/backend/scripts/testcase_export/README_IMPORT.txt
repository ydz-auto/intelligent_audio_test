============================================================
测试用例数据导入说明
============================================================

导出时间: 2026-08-23 11:04:43
数据库表行数: 1332
参考参数文件数: 0

导入步骤:
1. 确保目标数据库已创建表结构 (运行 flask app 初始化)
2. 执行 SQL 文件导入数据:
   psql -h <host> -U <user> -d <dbname> -f testcase_export_20260823_110443.sql
   或使用 pgAdmin / DBeaver 等工具执行 SQL 文件

3. 复制参考参数文件到目标环境的 static/ref_params/ 目录
   将 ref_params/ 目录下的内容复制到目标服务器的 static/ref_params/
   保持子目录结构 (用例ID/文件名)

注意:
- SQL 文件包含 DELETE FROM 语句，会先清空目标表再插入
- 如果目标数据库已有同ID数据，将被覆盖
- 参考参数文件路径在 config.rounds[].referenceParamsPath 中记录
  导入后需确保文件路径在目标环境可访问
